"""
数据降维：
    该维度是特征的数量， 降维则是  减少特征。

    特征选择：
        1. filter:  varianceFilter方差过滤( 过滤低于 指定方差值以下的特征， 删除相同值的特征)
            包名: feature_selection
        2. embed： 正则化、决策树

    PCA_ principal component analysis 主成分分析：





"""